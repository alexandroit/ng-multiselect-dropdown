## 4.0.3 (Angular 19)

- Fixed dropdown state handling so multiple component instances no longer leak open-state behavior across the application shell.
- Fixed source data remapping so object and primitive arrays stay aligned when settings initialize after input binding.
- Refined the dropdown trigger and panel layout so badges, caret positioning, and option lists render reliably in maintained Angular 19 builds.

## 4.0.2 (Angular 19)

- Standardized the root and published npm README to the shared ReviveJS Angular project format.
- Aligned package metadata with the maintained Angular 19 release line while keeping the versioned docs references intact.
- Refreshed the published package presentation without changing the component API.
